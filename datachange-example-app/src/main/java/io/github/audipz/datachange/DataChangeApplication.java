package io.github.audipz.datachange;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Example Spring Boot application that boots the DataChange framework together with demo entities.
 */
@SpringBootApplication
public class DataChangeApplication {

    public static void main(String[] args) {
        SpringApplication.run(DataChangeApplication.class, args);
    }
}
